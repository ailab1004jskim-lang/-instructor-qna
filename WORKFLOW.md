# 강사 질문방 빌드 로그

**시설 A/S 앱 하나만 있던 상태에서 시작해, 실제 서비스 주소가 나오기까지 밟은 순서**

작업 기간: 2026-08-15 ~ 08-16

각 단계에 **그때 실제로 입력한 문장**을 그대로 적어 두었습니다.
다음에 다른 프로젝트를 만들 때 같은 순서로 따라가시면 됩니다.

| 항목 | 내용 |
|---|---|
| 서비스 주소 | https://instructor-qna.vercel.app |
| 코드 저장소 | github.com/ailab1004jskim-lang/-instructor-qna |
| 구성 | Next.js · Neon(DB) · Resend(메일) · Vercel |
| 총 단계 | 6단계 / 커밋 7개 |

---

## 전체 흐름 한눈에

**[AI]** 는 AI에게 시킨 일, **[직접]** 은 사람이 직접 해야만 하는 일입니다.
직접 해야 하는 일은 계정 가입·비밀번호·결제처럼 남에게 맡길 수 없는 것들입니다.

| 단계 | 하는 일 | 누가 |
|---|---|---|
| 01 | 가진 것 파악하기 | **[AI]** |
| 02 | 기획 심문 — 만들기 전에 따져 묻기 | **[AI]** |
| 03 | 기획서 검토 후 보완 | **[AI]** |
| 04 | 코드 만들고 내 컴퓨터에서 실행 | **[AI]** |
| 05 | 외부 서비스 가입 — DB·메일 | **[직접]** |
| 06 | 인터넷에 올리기 | **[직접 + AI]** |

---

# 01. 가진 것 파악하기

**목표** — 새로 만들기 전에, 이미 있는 코드에 무엇이 들어 있는지부터 확인합니다.

### 입력한 문장

```
현재 프로젝트 분석해봐
```

### 결과

- 기존 A/S 앱의 구조·기능·기술 구성 정리
- 숨어 있던 문제 8건 발견 (메일 보안 취약점, 접수번호 중복 가능성 등)

### ✅ 여기서 얻은 것

이때 찾은 문제 2건을 새 프로젝트에 **복사하지 않고 고쳐서** 옮겼습니다.
기존 코드를 그냥 베꼈으면 같은 결함을 그대로 물려받았을 겁니다.

---

# 02. 기획 심문 — 만들기 전에 따져 묻기

**목표** — 가장 중요한 단계입니다. 코드를 한 줄도 쓰지 않고, 무엇을 만들지 먼저 확정합니다.

### 입력한 문장

```
/grill-me 지금 이 프로젝트를 기반으로 강사가 이용할수 플랫폼 재설계할려고 해
방식은 학생이 강사한테 질문 또는 의로 등등 이런 워크플로우 진행될수 있는 웹앱이야
```

`/grill-me` 는 "적당히 만들어주는" 대신 **계속 반박하고 캐묻는** 방식입니다.
3라운드에 걸쳐 12개 질문에 답하면서 만들 것이 확정됐습니다.

### 이 단계에서 결정된 것

| 물어본 것 | 정한 것 |
|---|---|
| 강사가 몇 명인가 | 1명 전용 |
| 학생이 로그인해야 하나 | 로그인 없음. 이메일로 개인 링크 발송 |
| 대화를 주고받나 | 질문 1 → 답변 1 (단발) |
| 다른 학생도 보나 | 전부 비공개 |
| 파일 첨부 되나 | 없음 |
| 상담 예약도 넣나 | 다음 버전으로 미룸 |

### ✅ 가장 크게 이득 본 결정

**파일 첨부를 뺀 것.**
단순히 기능 하나를 뺀 게 아니라, 사진 저장 공간이 필요 없어지면서 서버 선택이 자유로워졌습니다.
그 덕분에 나중에 Vercel에 그냥 올릴 수 있었습니다.

### ⚠️ 다음에 할 때

"이런 앱 만들어줘"라고만 하면 AI가 알아서 가정을 채웁니다.
그 가정이 틀리면 나중에 전부 뜯어고쳐야 합니다.

**만들기 전에 반드시 이 심문 단계를 거치세요. 30분 투자로 며칠을 아낍니다.**

---

# 03. 기획서 검토 후 보완

**목표** — 기획서가 나왔다고 바로 만들지 않습니다. 한 번 더 검토시켜 구멍을 찾습니다.

### 입력한 문장

```
새 레포 스케폴딩전에 문서 먼저 검토해줘
```

```
12건 반영해서 PRD v1.1로 갱신해
```

### 찾아낸 치명적 구멍

- 제출 완료 화면에 개인 링크를 띄우면, 남의 이메일 주소를 넣은 사람이 그 사람 질문을 전부 볼 수 있었음
- 스팸 질문을 지울 방법이 없었음
- 링크 주소만 바꾸면 다른 학생 질문이 열릴 수 있었음

### ✅ 여기서 얻은 것

세 가지 모두 **코드를 쓰기 전에** 발견해서 고쳤습니다.
만들고 나서 발견했다면 다시 설계해야 했습니다.

---

# 04. 코드 만들고 내 컴퓨터에서 실행

**목표** — 확정된 기획서대로 실제 프로그램을 만들고, 먼저 내 컴퓨터에서 돌려봅니다.

### 입력한 문장

```
새 레포 만들고 스케폴딩 시작해 일단 로컬배포
```

"**일단 로컬배포**"가 중요합니다.
바로 인터넷에 올리지 않고 내 컴퓨터에서 먼저 확인하겠다는 뜻입니다.
문제가 있어도 아무도 안 보는 상태에서 고칠 수 있습니다.

### 그다음 요청한 것

```
Resend 연결(이메일 실제 발송), 디자인 손보기, 실제 배포.순으로 진행해줘
```

할 일을 **순서대로 나열**하면 그 순서대로 진행됩니다.
한 번에 하나씩 확인하며 갈 수 있습니다.

### ⚠️ 중간에 걸린 것 — git 이름 설정

코드 저장 기록에 남길 이름·이메일이 설정돼 있지 않아 한 번 멈췄습니다.
이렇게 요청해서 해결했습니다.

```
user.name = passionAIlab / user.email = ailab1004jskim@gmail.com 으로 변경해줘
```

---

# 05. 외부 서비스 가입 — 데이터베이스와 메일

**목표** — 코드만으로는 안 됩니다. 데이터를 담을 곳과 메일을 보낼 곳이 따로 필요합니다.

### ⚠️ 여기부터는 직접

가입·비밀번호 입력은 AI가 대신할 수 없습니다.
다만 **무엇을 어디서 눌러야 하는지는 물어보면 됩니다.**

### 모를 때 이렇게 물어봤습니다

```
.env의 DATABASE_URL에 대한 지식이 없는 초보야 자세한 설명과
비전공도 이해 할수 있도록 가이드 해주면 답변할께 진행해
```

**"초보야, 비전공자도 이해하게 설명해줘"** 를 붙이면 설명 수준이 완전히 달라집니다.
모르는 용어가 나오면 그때그때 이렇게 요청하세요.

### 가입한 곳 두 군데

| 서비스 | 역할 | 받아온 것 |
|---|---|---|
| Neon | 질문·답변을 저장하는 데이터베이스 | 연결 주소 2개 |
| Resend | 이메일 발송 | `re_` 로 시작하는 키 |

### ❌ 두 번 실수한 것 — 파일을 잘못 골랐음

받아온 주소와 키를 `.env.example` 에 넣었는데, 이건 **남에게 보여주는 견본 파일**입니다.
실제 값은 `.env` 에 넣어야 합니다.

**구분법:** 이름에 `example` 이 붙어 있으면 견본입니다. 진짜 비밀번호는 절대 넣지 마세요.

```
.env          ← 진짜 값. 인터넷에 안 올라감
.env.example  ← 견본. GitHub에 올라감. 가짜 값만
```

### ❌ 알아둘 것 — 메일 발송 제한

Resend는 본인 도메인을 등록하기 전까지 **가입한 본인 이메일 주소로만** 발송됩니다.

- 강사에게 오는 알림 → 잘 옴
- 학생에게 가는 알림 → **안 감**

학생에게도 보내려면 도메인을 구입해 등록해야 합니다.

---

# 06. 인터넷에 올리기

**목표** — 내 컴퓨터에서만 되던 것을 누구나 접속할 수 있는 주소로 만듭니다.

### 순서

1. **GitHub에 코드 올리기** — 저장소를 만들고 `git push`
2. **Vercel 가입** — GitHub 계정으로 로그인
3. **저장소 연결** — Import 누르면 자동 인식
4. **환경변수 입력** — Neon 주소, Resend 키, 비밀번호 등 7개
5. **Deploy** — 1~3분 대기

### ❌ 가장 오래 막힌 지점

사이트는 떴는데 **"문제가 발생했습니다"** 화면만 나왔습니다.

원인은 환경변수에 값을 붙여넣을 때 **앞뒤 큰따옴표가 같이 들어간 것**이었습니다.

```
잘못됨:  "postgresql://neondb_owner:...require"
         ↑                                    ↑ 따옴표가 붙어 있음

맞음:    postgresql://neondb_owner:...require
```

값을 다시 넣고 재배포하니 바로 해결됐습니다.

### ⚠️ 기억할 규칙

**Vercel은 배포하는 순간의 환경변수를 고정합니다.**

나중에 값을 바꾸면 반드시 아래를 눌러야 반영됩니다.

```
Deployments → 맨 위 줄 → ⋯ → Redeploy
```

이때 "Use existing Build Cache" 체크박스가 있으면 **해제**하세요.

### 막혔을 때 이렇게 요청하면 됩니다

```
Deploy버튼을 찾을수 없어 확인할수 있게 비전공자도 알수 있게 단계적으로 알려줘
```

```
대신 해줘
```

직접 누르기 어려운 건 "대신 해줘"라고 하면 됩니다.
실제로 이때 AI가 코드를 하나 올려서 재배포를 걸었습니다.

다만 **로그인·가입·결제는 대신 못 합니다.**

---

# ✅ 완료

주소가 나오고 질문 유형 4종이 화면에 뜨면 성공입니다.

---

# 다음에 다른 프로젝트를 만들 때

순서대로 이 문장들을 쓰시면 같은 흐름으로 진행됩니다. 그대로 복사해서 쓰셔도 됩니다.

| 단계 | 입력할 문장 |
|---|---|
| 1. 파악 | `현재 프로젝트 분석해봐` |
| 2. 심문 | `/grill-me <만들고 싶은 것을 한두 문장으로>` |
| 3. 검토 | `만들기 전에 기획 문서 먼저 검토해줘` |
| 4. 보완 | `지적한 내용 반영해서 문서 갱신해` |
| 5. 제작 | `새 레포 만들고 스케폴딩 시작해 일단 로컬배포` |
| 6. 마무리 | `<할 일 A>, <할 일 B>, <할 일 C> 순으로 진행해줘` |
| 중단 시 | `비전공자도 알 수 있게 단계적으로 알려줘` |
| 기록 남기기 | `/handoff 현재까지 작업과정 정리` |

---

# 배포 전 확인 목록

- [ ] 비밀번호·키가 `.env` 에 있는지 (`.env.example` 아님)
- [ ] 환경변수 값에 큰따옴표·앞뒤 공백이 없는지
- [ ] 운영용 비밀번호를 로컬과 다르게 새로 만들었는지
- [ ] 환경변수를 바꾼 뒤 재배포(Redeploy)를 눌렀는지
- [ ] 대화 중에 노출된 키는 새로 발급받아 교체했는지
- [ ] 사이트 접속해서 실제로 화면이 뜨는지 눈으로 확인했는지

---

# 아직 남은 일

| 할 일 | 왜 |
|---|---|
| Resend 키·Neon 비밀번호 교체 | 작업 중 대화창에 노출됨 |
| 도메인 등록 | 학생에게 메일이 가려면 필요 |
| 테스트 질문 2건 삭제 | 강사 화면에서 직접 삭제 가능 |
| 관리자 비밀번호 재변경 | 첫 로그인 후 설정 화면에서 |
| 운영 사이트 최종 확인 | 강사 로그인 + 질문 제출 실제 테스트 |

---

# 함께 있는 문서

같은 폴더(`D:\07.AI(웹)\instructor-qna`) 안에 있습니다.

| 파일 | 내용 |
|---|---|
| `PRD.md` | 무엇을 왜 만들었는지 (기획서) |
| `DEPLOY.md` | 배포 절차 상세 |
| `README.md` | 실행 방법 |
| `AGENTS.md` | 코드를 고칠 때 지켜야 할 규칙 |
| `WORKFLOW.md` | 이 문서 |
