# PRD — 강사 Q&A 플랫폼 (가칭)

작성일: 2026-08-15 · 버전: **v1.1** (설계 검토 반영)
전신: `as 센터` (시설 A/S 접수센터) — 자산만 이식, 별도 신규 레포

> **v1.0 → v1.1 변경 요약** — 검토에서 발견된 12건 반영. 상세는 §12.
> 핵심: **완료 화면의 개인 링크 노출 제거**(이메일 오타로 타인 질문 열람 가능했음),
> 질문 삭제 기능 추가, 조회 시 소유권 검증 명시, 텍스트 개행 보존 규칙 추가.

---

## 1. 배경 및 목표

강사 1명이 담당 학생들의 질문을 웹으로 받아 답변하는 비동기 Q&A 도구.
현재는 카톡·전화·수업 직후 질문이 흩어져 누락·중복이 발생한다. 이를 단일 창구로 모은다.

**목표**
- 학생: 로그인 없이 1분 내 질문 제출, 답변 도착 시 이메일 알림 수신
- 강사: 대기 질문을 한 화면에서 확인하고 답변, 누락 제로
- 질문 품질 확보: 자유서술 대신 **구조화 3필드**로 정보 부족 문제를 원천 차단

**비목표 (v1 제외)**
- 상담·면담 예약 → **v2**
- 왕복 대화(스레드) / 강사 되묻기
- 파일·이미지 첨부
- 질문 공개(FAQ 게시판)
- 강좌·분반 구분, 긴급도
- 다중 강사(멀티테넌시) — **v2에서도 계획 없음**

---

## 2. 사용자 및 접근 모델

| 역할 | 인증 | 설명 |
|---|---|---|
| 학생 | 없음 (이메일로 발송된 개인 링크) | **이메일 수신 = 본인 증명** |
| 강사 | 단일 계정, 비밀번호만 | 관리 주체 1인. 세션 쿠키 유지 |

### 학생 식별 설계 (핵심)

계정·비밀번호 없이 신원을 고정하는 방식:

1. 학생이 질문 제출 시 **이메일 필수** 입력
2. `Student` 레코드를 이메일 기준 upsert → 영구 `token`(랜덤 32hex) 보유
3. **개인 링크 `/my/<token>`은 오직 이메일로만 발송한다**
4. 이 링크로 본인 질문 전체 + 답변 열람
5. 링크 분실 시 `/link`에서 이메일 입력 → 그 주소로 재발송

### ⚠ 개인 링크를 화면에 절대 노출하지 않는 이유

v1.0 설계는 제출 완료 화면에 개인 링크를 크게 띄우려 했다. **이건 인증 우회 구멍이다.**

```
학생 A가 학생 B의 이메일을 입력 (오타 또는 고의)
  → B의 기존 Student 레코드에 A의 질문이 붙음
  → 완료 화면이 A에게 B의 개인 링크를 그대로 노출
  → A가 B의 전체 질문 열람
```

오타 한 번으로도 터지고, 고의라면 남의 이메일 주소만 알면 아무 질문이나 넣고 완료 화면에서 링크를 얻는다. "링크 소유 = 본인 증명"이라는 전제 자체가 무너진다.

**따라서 링크는 이메일 채널로만 전달한다.** 이메일 수신함에 접근할 수 있는 사람 = 그 주소의 소유자라는 것이 이 설계의 유일한 신뢰 근거이며, 화면 노출은 그 근거를 통째로 무효화한다.

남는 잔여 위험은 "오타로 남의 주소를 넣어 그 사람이 모르는 질문 1건을 자기 목록에서 보게 되는" 정도로 축소된다 (§10③).

**중요**: 전신 앱의 `이름 + 연락처` 조회 경로(`lookup/results`)는 **이식하지 않는다.**
같은 반 학생끼리 이름·연락처를 서로 알기 때문에 그 경로를 남기면 개인 링크로 잠근 문 옆에 뒷문을 뚫는 꼴이 된다. 조회 경로는 토큰 단일화.

---

## 3. 핵심 기능

### 3.1 질문 제출 (학생용, 공개)

| 필드 | 컬럼 | 타입 | 필수 | 검증 |
|---|---|---|---|---|
| 성명 | `submitterName` | 텍스트 | ✔ | 2자 이상 |
| 이메일 | (→ `Student.email`) | email | ✔ | 형식 검증 |
| 질문 유형 | `topicId` | 드롭다운 (강사 관리 목록) | ✔ | active한 `Topic`만 |
| 어디서 막혔나요 | `context` | 텍스트 | ✔ | **5자 이상** |
| 어떻게 시도해봤나요 | `tried` | 장문 텍스트 | ✔ | **10자 이상** |
| 어떤 결과가 나왔나요 / 무엇이 이해 안 되나요 | `problem` | 장문 텍스트 | ✔ | **10자 이상** |

**구조화 3필드가 v1 설계의 핵심.**
단발(왕복 불가) + 첨부 없음 구조에서 "이거 왜 안 돼요?" 류 질문이 들어오면 강사가 답할 수도, 되물을 수도 없다. 자유서술 한 칸을 `context / tried / problem` 세 칸으로 강제 분해해 이 실패 모드를 구조로 막는다.

**최소 길이 검증이 이 장치의 실효성을 담보한다.** 검증이 없으면 세 칸을 `ㅁㄴㅇㄹ`로 채워 무력화할 수 있다. 각 필드의 안내 문구(placeholder)에 좋은 예시를 넣어 길이 요구를 자연스럽게 만든다.

**텍스트 서식 규칙** — 첨부를 제외했으므로 학생이 붙여넣는 것은 대부분 에러 메시지·코드·수식이다. 개행이 뭉개지면 읽을 수 없다.
- 입력: 일반 `textarea` (마크다운 파서 도입하지 않음)
- **표시: 모든 본문 렌더링에 `white-space: pre-wrap` 적용** — 학생 조회 화면, 강사 상세 화면, 이메일 본문 전부
- 본문 영역에 등폭 폰트 대안 스타일 제공 (`font-mono` 토글 또는 코드 블록 스타일)
- 전신 앱도 `src/lib/email.ts:45`에서 `white-space:pre-wrap`을 쓰고 있으니 패턴 그대로 가져온다

**제출 후 흐름**
- **질문번호** 발급: `Q-YYYY-NNNN-XXX`
- `/submitted/[publicNo]` 완료 화면에는 **질문번호와 안내 문구만** 표시:
  > 접수되었습니다. 질문번호 `Q-2026-0001-ABC`
  > 입력하신 이메일로 **내 질문 확인 링크**를 보냈습니다.
  > 메일이 오지 않으면 `/link`에서 재발송하세요.
- **완료 화면에 개인 링크를 출력하지 않는다** (§2 참조)
- 모바일 우선

### 3.2 내 질문 조회 (학생용, 토큰)

| 경로 | 설명 |
|---|---|
| `/my/[token]` | 본인 질문 목록 (상태 배지 + 제출일) |
| `/my/[token]/[publicNo]` | 질문 전문 + 강사 답변 |
| `/link` | 링크 재발송 (이메일 입력 → 해당 주소로 개인 링크 메일) |

**⚠ 상세 조회는 반드시 소유권을 함께 검증한다.**

토큰은 학생을 특정할 뿐, 그것만으로 특정 질문에 대한 권한이 되지 않는다. 아래처럼 구현하면 토큰 하나로 전 학생의 질문이 열린다:

```ts
// ✗ 금지 — 소유권 미검증
const q = await prisma.question.findUnique({ where: { publicNo } });
```

```ts
// ✓ 필수 — 토큰의 학생 소유 건만
const student = await prisma.student.findUnique({ where: { token } });
if (!student) notFound();
const q = await prisma.question.findFirst({
  where: { publicNo, studentId: student.id },
});
if (!q) notFound();
```

URL 식별자는 `publicNo`로 통일한다 (사람이 읽을 수 있고 내부 `cuid`를 노출하지 않음).

**`/link` 재발송 시 주의** — 등록되지 않은 이메일을 입력해도 동일한 응답("메일을 보냈습니다")을 반환한다. 응답을 달리하면 가입 여부 확인 도구가 된다.

### 3.3 강사 대시보드

- **로그인**: 비밀번호 단일 입력
- **대기 목록이 기본 화면**: 미답변 질문을 상단에, 오래된 순 정렬. 답변 완료 건은 별도 탭
- **집계 카드**: 대기 / 오늘 접수 / 이번 주 답변 / 전체
- **필터**: 질문 유형 · 답변 여부 · 기간 · 학생명 검색(`Question.submitterName` 대상)
- **질문 상세 + 답변 작성**: 3필드 전문(`pre-wrap`) 표시, 답변 textarea, 저장 시 `answeredAt` 기록 + 학생에게 알림 메일 발송
- **답변 수정**: 재저장 가능. 수정 시 학생 재알림 여부는 체크박스로 선택
- **질문 삭제** — 아래 참조
- **질문 유형 관리**: 추가 / 라벨·안내문구 수정 / 비활성화. 삭제 불가(참조 무결성)
- **설정**: 서비스명, 강사 알림 수신 이메일(쉼표 다중), 비밀번호 변경

**질문 삭제가 선택이 아닌 이유**

§10①에서 접근 제한을 두지 않기로 결정했다. 그 결정의 직접적 귀결로 **스팸·오제출 질문이 목록에 유입될 수 있고, 지울 수단이 없으면 대시보드가 영구 오염된다.** 레이트리밋은 유입 속도만 늦출 뿐 이미 들어온 건에 대해 아무것도 하지 않는다.

- 강사만 삭제 가능. 확인 단계(2단계 클릭) 필수
- 하드 삭제 (첨부가 없으므로 파일 정리 불필요)
- 전신 앱 `src/app/actions/admin.ts:83-99`의 `deleteRequest` 패턴 이식. 단 사진 정리 로직은 제거
- 삭제해도 `Counter`는 되돌리지 않는다 → 질문번호 재사용 없음 (§8② 참조)

### 3.4 이메일 알림 (양방향)

| 트리거 | 수신자 | 내용 |
|---|---|---|
| 신규 질문 제출 | 강사 | 학생명 · 유형 · 질문 요약 + 답변 페이지 링크 |
| 답변 저장 | 학생 | "답변이 도착했습니다" + 개인 링크 |
| 링크 재발송 요청 | 학생 | 개인 링크 |

- 발송: Resend
- **HTML 이스케이프 필수** (§8① 참조)
- 본문의 학생 입력 영역에 `white-space:pre-wrap` 적용

**⚠ 볼륨 — 월 한도가 아니라 일 한도가 병목이다**

| 구간 | 계산 | 결과 |
|---|---|---|
| 평상시 월 | 학생 50명 × 주 2질문 × 2통 | 월 ~800통 |
| **피크 데이** | 과제 마감 전날, 학생 50명이 하루에 몰림 × 2통 | **하루 100통** |

Resend 무료 티어는 월 한도(3,000통)와 **별도로 일일 한도**를 둔다. 월 총량은 여유로운데 **가장 중요한 날에 정확히 경계선에 걸린다.**

**착수 전 필수 확인**: Resend 현행 무료 티어의 일일 발송 한도 실측 (정책이 변동될 수 있으므로 문서 기억에 의존하지 말 것).

**완화책** (한도가 실제로 빠듯하면 적용):
1. 강사 알림을 건별 즉시 발송 → **N분 배치 묶음 발송**으로 전환 (통수 절반 이하)
2. 강사 알림 비활성 옵션 (강사가 대시보드를 주기적으로 보는 운영이라면 불필요)
3. 유료 전환 ($20/월). 학생 100명 넘어가면 어차피 필요

**학생 알림은 절대 줄이지 않는다** — 이게 없으면 학생이 플랫폼을 우회한다(§11 결정 이력).

---

## 4. 상태 모델

**2단계. enum 없음 — `answeredAt` null 여부로 파생.**

```
대기 (answeredAt == null)  →  답변완료 (answeredAt != null)
```

- 강사가 답변을 저장하면 자동 전이. 별도 상태 변경 버튼 없음
- 전신 앱의 `Status` enum 6단계 · `StatusLog` 테이블 · `StatusStepper` 컴포넌트 · `StatusControl` 전부 **미이식**

---

## 5. 기술 스택 및 인프라

| 구성 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) — 전신과 동일 |
| ORM/DB | Prisma 7 + PostgreSQL |
| 스타일 | Tailwind CSS v4 + 전신 디자인 토큰 이식 |
| 이메일 | Resend |
| 배포 | **미정** — 플랫폼 중립 설계 |

**파일 첨부를 v1에서 제외한 결과, 인프라 제약이 사라졌다.**
전신 앱은 사진을 로컬 디스크(Railway Volume)에 저장해 단일 인스턴스에 묶여 있었다. v1은 순수 텍스트 앱이므로 Volume 불필요 → 수평 확장 가능 → Vercel·Railway 어느 쪽이든 코드 변경 없이 배포 가능.

배포처는 나중에 결정한다. 결정을 미룰 수 있도록 **디스크 쓰기 코드를 일절 도입하지 않는다.**

### ⚠ 플랫폼 중립의 예외 — DB 커넥션

전신 앱의 `src/lib/prisma.ts`는 `PrismaPg` 어댑터로 Postgres에 **직접 연결**한다. 상시 구동 컨테이너(Railway)에서는 문제없지만, **서버리스(Vercel)에서는 인스턴스마다 커넥션을 열어 풀이 고갈된다.**

배포처 확정 시 반드시 처리:
- **Railway** → 현행 그대로
- **Vercel** → 커넥션 풀러를 경유하는 연결 문자열 사용 (예: Neon의 pooled 엔드포인트). Prisma 어댑터 설정도 함께 확인

이 한 가지만 배포처에 종속된다. 나머지 코드는 중립.

### IP 주소 획득

레이트리밋(§10①)이 `submitterIp`에 의존하므로 획득 방법을 고정한다:

```ts
const h = await headers();
const ip =
  h.get("x-forwarded-for")?.split(",")[0]?.trim() ||
  h.get("x-real-ip") ||
  null;
```

`x-forwarded-for`는 프록시 체인이 쉼표로 이어지므로 **첫 항목**을 취한다. 배포처마다 헤더가 다를 수 있어 폴백을 둔다.

---

## 6. 데이터 모델

```prisma
model Student {
  id        String     @id @default(cuid())
  email     String     @unique          // 신원 키
  name      String                       // 최근 제출 시 이름으로 갱신 (표시용)
  token     String     @unique          // 개인 링크용 랜덤 32hex
  createdAt DateTime   @default(now())
  questions Question[]
}

model Topic {                            // 전신의 Category 후신
  id        String     @id @default(cuid())
  label     String
  hint      String?
  active    Boolean    @default(true)
  createdAt DateTime   @default(now())
  questions Question[]
}

model Question {
  id            String    @id @default(cuid())
  publicNo      String    @unique       // Q-2026-0001-ABC · URL 식별자
  studentId     String
  student       Student   @relation(fields: [studentId], references: [id])
  topicId       String
  topic         Topic     @relation(fields: [topicId], references: [id])

  submitterName String                   // 제출 시점 이름 스냅샷 (아래 주석 참조)

  context       String                   // 어디서 막혔나
  tried         String                   // 어떻게 시도했나
  problem       String                   // 어떤 결과 / 무엇이 이해 안 되나

  answer        String?
  answeredAt    DateTime?

  submitterIp   String?                  // 레이트리밋용
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@index([answeredAt])
  @@index([createdAt])
  @@index([studentId])
  @@index([topicId])
  @@index([submitterIp, createdAt])      // 레이트리밋 조회용
}

model Counter {                          // 질문번호 시퀀스 (§8② 참조)
  year  Int @id
  value Int @default(0)
}

model Settings {
  id                Int     @id @default(1)
  serviceName       String  @default("강사 질문방")
  notifyEmail       String?              // 강사 알림 수신, 쉼표 다중
  adminPasswordHash String?
}
```

**`submitterName` 스냅샷을 두는 이유**
`Student.name`은 같은 이메일로 재제출할 때마다 덮어써진다. 그러면 (a) 과거 질문의 당시 이름이 소실되고, (b) 강사 대시보드의 학생명 검색이 매번 `Student` 조인을 요구한다. 제출 시점 이름을 `Question`에 복사해 두면 둘 다 해결되고 비용은 컬럼 하나다. `Student.name`은 개인 링크 페이지 인사말 등 "현재 이름" 표시용으로만 쓴다.

**`answeredBy` 필드는 두지 않는다** — v1.0 초안에 "v2 다중 강사 대비"로 있었으나, 다중 강사는 §1 비목표이며 v2(예약)와도 무관하다. 근거 없는 선투자.

**초기 시드**: `Topic` 4종 — 강의 내용 / 과제 / 행정·수강 / 기타

---

## 7. 화면 목록

| 경로 | 접근 | 설명 |
|---|---|---|
| `/` | 공개 | 질문 제출 폼 (모바일 우선) |
| `/submitted/[publicNo]` | 공개 | 제출 완료 — **질문번호 + 메일 발송 안내만.** 링크 노출 금지 |
| `/my/[token]` | 토큰 | 내 질문 목록 |
| `/my/[token]/[publicNo]` | 토큰 + **소유권 검증** | 질문 상세 + 답변 |
| `/link` | 공개 | 개인 링크 재발송 (이메일 입력) |
| `/admin/login` | 공개 | 강사 로그인 |
| `/admin` | 강사 | 대기 질문 목록 + 집계 카드 + 필터 |
| `/admin/questions/[id]` | 강사 | 질문 상세 + 답변 작성 + 삭제 |
| `/admin/topics` | 강사 | 질문 유형 관리 |
| `/admin/settings` | 강사 | 서비스명 / 알림 이메일 / 비밀번호 |

---

## 8. 전신 앱에서 이식할 것 · 고칠 것

### 그대로 이식

| 파일 | 비고 |
|---|---|
| `src/lib/prisma.ts` | `PrismaPg` 어댑터 패턴. **단 배포처가 서버리스면 §5 예외 적용** |
| `src/lib/auth.ts` | **강사 1명 전용이라 그대로 맞음.** scrypt + timingSafeEqual + 비번 종속 세션 토큰(비번 변경 시 전 세션 자동 무효화) |
| `src/lib/settings.ts` | 싱글톤 upsert 패턴 |
| `src/app/globals.css` | 디자인 토큰 · `card` / `btn-*` / `badge` 유틸 |
| `(public)` / `admin/(protected)` 라우트 그룹 구조 | |
| `LoginForm` · `PasswordForm` · `SettingsForm` · `AdminNav` | 거의 무수정 |
| `CategoryManager` → `TopicManager` | 라벨만 교체 |
| `DeleteRequestButton` → `DeleteQuestionButton` | 2단계 확인 UX 그대로 |
| `formatDateTime` / `formatDateTimeKorean` | `constants.ts`에서 발췌 |

### 고쳐서 이식 — 전신 앱의 알려진 결함 2건

**① 이메일 HTML 인젝션** (`src/lib/email.ts:34-47`)
사용자 입력(`detail`, `branchName`, `requesterName`)을 이스케이프 없이 HTML 템플릿에 보간하고 있다. 그대로 베끼면 결함까지 복제된다.

```ts
function escapeHtml(s: string) {
  return s.replace(/[&<>"']/g, (c) => (
    { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]!
  ));
}
```
학생 입력이 들어가는 **모든** 보간 지점에 적용 — 이름·질문 3필드·유형 라벨. 강사 답변 본문도 마찬가지(강사가 신뢰 주체여도 `<`가 든 코드를 붙여넣으면 레이아웃이 깨진다).

**② 질문번호 시퀀스 경합·재사용** (`src/lib/requestNo.ts:17-21`)
전신은 `prisma.request.count()` 결과 + 1로 순번을 만든다. 삭제 기능이 있어 카운트가 줄면 번호가 재사용되고, 동시 제출 시 경합이 난다.

→ `Counter` 테이블로 원자적 증가:
```ts
const { value } = await prisma.counter.upsert({
  where: { year },
  update: { value: { increment: 1 } },
  create: { year, value: 1 },
});
// Q-{year}-{String(value).padStart(4,"0")}-{randomSuffix()}
```
랜덤 접미사(`SUFFIX_CHARS`, 혼동 문자 0/O/1/I/L 제외)는 전신 로직 그대로 유지.

**→ 전신의 P2002 재시도 루프는 이식하지 않는다.**
`src/app/actions/requests.ts:72-104`에는 접수번호 충돌에 대비한 3회 재시도 루프가 있다. `Counter`가 순번의 유일성을 원자적으로 보장하므로 `publicNo` 전체가 유일해지고, 이 루프는 죽은 코드가 된다. 그대로 베끼면 불필요한 복잡도만 남는다.

### 미이식 (폐기)

`uploads.ts` (sharp) · `urgency.ts` · `Branch` 모델 + `BranchManager` · `StatusLog` + `StatusStepper` + `StatusControl` · `admin/print/[id]` A4 인쇄 뷰 · `api/photos/[name]` · `lookup/results` 이름+연락처 조회 · `next.config.ts`의 `bodySizeLimit: 110mb` · `requests.ts`의 P2002 재시도 루프

---

## 9. 개인정보 취급

학생 **실명 + 이메일 주소 + 질문 내용**을 보관한다. 질문에는 학업 이해도·개인 사정이 담길 수 있어 민감도가 낮지 않다.

**v1 최소 정책**
- 수집 항목과 목적을 제출 폼 하단에 한 줄로 고지 (이름·이메일: 본인 확인 및 답변 통지)
- **보관 기간: 학기 종료 후 6개월.** 이후 강사가 일괄 삭제
- 일괄 삭제 수단: v1은 DB 직접 실행으로 충분. 운영이 굳으면 `/admin/settings`에 "N개월 이전 질문 일괄 삭제" 버튼 추가 (v1.1 후보)
- 학생의 삭제 요청 창구: 강사 이메일 (별도 기능 없음)

기관 소속으로 운영한다면 해당 기관의 개인정보 처리방침을 따를 것. 이 문서는 최소선만 정한다.

---

## 10. 남은 리스크 (수용됨, 인지 필요)

**① 외부인·봇 제출 가능**
접근 제한을 두지 않기로 결정했다. 완화책은 레이트리밋뿐:
- **IP당 시간당 5건** — `Question` 테이블을 `submitterIp` + `createdAt`으로 카운트 (별도 테이블 불필요, `@@index([submitterIp, createdAt])` 활용)
- **이메일당 하루 10건**
- 초과 시 **저장 자체를 거부**한다. 저장 후 표시만 막으면 DB와 강사 알림 메일이 그대로 오염된다
- 실질 유인은 낮다 — 답변이 오직 제출자 이메일로만 가므로 봇이 얻을 것이 없다
- 오염이 실제로 발생하면 v1.1에서 수강 참여코드 1줄 추가로 즉시 차단 가능하도록 설계를 열어둔다
- 유입된 건은 강사 삭제 기능(§3.3)으로 정리

**② 되묻기 불가**
단발 구조. 정보가 부족한 질문은 강사가 답변란에 "이러이러한 정보를 담아 다시 질문해주세요"라 쓰고 학생이 새 질문을 작성하는 수밖에 없다. 맥락이 끊긴다.
구조화 3필드 + 최소 길이 검증이 발생 빈도를 크게 낮추지만 0으로 만들지는 못한다. 운영 후 빈도가 높으면 `Message` 테이블 도입을 재검토한다.

**③ 이메일 오타 시 타인 목록 오염**
학생 A가 오타로 학생 B의 주소를 입력하면 A의 질문이 B의 목록에 나타난다. 개인 링크를 이메일로만 보내므로(§2) **A가 B의 질문을 보는 방향은 차단**되지만, B가 A의 질문 1건을 보게 되는 방향은 남는다.
완화: 이메일 입력란 재확인 안내, 발송 메일에 "본인이 하지 않은 질문이 보이면 강사에게 알려주세요" 문구. 강사가 삭제로 정리.

**④ 개인 링크 유출 = 질문 전체 노출**
학생이 링크를 단톡방에 잘못 붙여넣으면 그 학생의 전체 질문이 열린다. 완화: 발송 메일에 "이 링크는 본인만 사용" 경고. 링크 재발급(토큰 회전) 기능은 v1.1 후보.

**⑤ 이메일 미수신 시 학생이 막힘**
링크를 화면에 안 띄우므로, 메일이 스팸함으로 가면 학생은 조회 경로를 잃는다. 완화: 완료 화면에 `/link` 재발송 안내를 명시하고, 발신 도메인 설정(SPF/DKIM)을 배포 시 점검한다.

---

## 11. v2 — 상담·면담 예약

v1에서 의도적으로 제외. **질문과 성격이 정반대인 별도 앱**이기 때문:

| | 질문 | 예약 |
|---|---|---|
| 자원 | 무한(비동기) | **유한(강사 시간)** |
| 경합 | 없음 | **있음(동시 클릭)** |
| 취소 | 개념 없음 | 필수 |
| 리마인더 | 불필요 | 필수(노쇼 방지) |

v1을 운영하며 예약 수요를 검증한 뒤 착수한다. 착수 시점에 먼저 답해야 할 것:

1. 강사 가능시간 정의 — 주간 반복 규칙 vs 매번 수동 개설
2. 슬롯 길이 (30분 고정 여부)
3. **동시 예약 경합** — `@@unique([slotStart])` 필수. 없으면 이중 예약 발생
4. 취소 규칙 — 마감 시한, 취소 시 슬롯 재개방 여부
5. 장소 — 온라인(링크) / 오프라인(강의실) / 둘 다
6. 리마인더 — 하루 전 메일. **크론 필요** (배포처 선택에 영향)
7. 슬롯 부족 시 대기열 — 없으면 "매주 광클 경쟁"이 된다
8. **이메일 볼륨 재계산** — 예약 1건당 3통(확인·강사알림·리마인더). §3.4의 일일 한도 여유가 더 줄어든다

**자체 구현이 정당화되는 조건**은 아래 중 하나 이상일 때뿐이다. 아니면 Calendly 링크 연결(개발 0일)이 이긴다.
- 예약을 기존 질문과 연결해야 함
- 강사가 질문+예약을 한 대시보드에서 봐야 함
- 학생 개인 링크 흐름 안에서 예약이 끝나야 함

**v2를 위한 선투자는 하지 않는다.** 스키마에 예비 필드를 두지 않으며, v2 착수 시 마이그레이션으로 추가한다.

---

## 12. v1.0 → v1.1 변경 이력 (검토 반영 12건)

| # | 심각도 | 내용 | 반영 위치 |
|---|---|---|---|
| 1 | **치명** | 완료 화면의 개인 링크 노출 제거 — 이메일 오타·도용으로 타인 질문 전체 열람 가능했음. 링크는 이메일 채널로만 전달 | §2, §3.1, §7 |
| 2 | **치명** | 질문 삭제 기능 추가 — "접근 제한 없음" 결정의 필연적 귀결인데 누락돼 있었음 | §3.3 |
| 3 | **치명** | 상세 조회 시 소유권 검증(`studentId` 대조) 명시. URL 식별자를 `publicNo`로 통일 | §3.2, §7 |
| 4 | 중요 | 텍스트 개행 보존(`pre-wrap`) 규칙 추가 — 첨부를 뺐는데 코드·에러 메시지 표현 수단이 없었음 | §3.1, §3.4 |
| 5 | 중요 | Resend **일일** 한도 리스크 추가. 피크 데이 100통 경계 + 완화책 3안 | §3.4 |
| 6 | 정리 | `answeredBy` 필드 삭제 — 다중 강사는 비목표이고 "v2 선투자 안 함" 원칙과 모순 | §6, §11 |
| 7 | 정리 | `Question.submitterName` 스냅샷 추가 — `Student.name` 덮어쓰기로 과거 이름 소실 + 검색 조인 문제 해결 | §6 |
| 8 | 정리 | IP 획득 방법 명시 (`x-forwarded-for` 첫 항목 → `x-real-ip` 폴백) | §5 |
| 9 | 정리 | P2002 재시도 루프를 **미이식 목록에 추가** — `Counter` 도입으로 죽은 코드가 됨 | §8 |
| 10 | 정리 | 3필드 최소 길이 검증 추가 — 없으면 구조화 장치가 무력화됨 | §3.1 |
| 11 | 정리 | 서버리스 커넥션 풀을 "플랫폼 중립의 예외"로 명시 | §5 |
| 12 | 정리 | 개인정보 보관·파기 정책 신설 (학기 종료 후 6개월) | §9 |

추가 반영: 레이트리밋 초과 시 **저장 거부** 명시(§10①), `/link` 응답 균일화로 계정 열거 방지(§3.2), 이메일 미수신 리스크 신설(§10⑤), 기본 서비스명 변경.

---

## 13. 결정 이력 (그릴링 세션 2026-08-15)

| 결정 | 선택 | 이유 |
|---|---|---|
| 테넌시 | 강사 1명 전용 | 전신의 `Settings` 싱글톤 · 단일 비번 인증 그대로 재사용 가능 |
| 코드 재사용 | 신규 레포 + 자산만 복사 | 전신 900줄이 A/S 도메인 전용. 개조보다 이식이 쌈. A/S 앱은 그대로 보존 |
| 학생 인증 | 이메일 + 개인 링크 토큰 (**이메일 채널 전용**) | 계정 없이 신원 고정. 이름+연락처 조회의 사생활 누출 문제를 동시 해결 |
| 대화 모델 | 단발 | 왕복 수요가 낮다고 판단. 대신 구조화 3필드로 정보 부족을 사전 차단 |
| 공개 범위 | 전부 비공개 (1:1) | FAQ 게시판은 익명화·검색·태그가 딸려와 스코프 2배 |
| 알림 | 이메일 + 개인 링크 병행 | 학생이 답변 도착을 모르면 플랫폼을 우회(카톡·전화)한다 |
| 첨부 | 없음 | Volume 종속 해소 → 배포처 자유. 인프라 난이도 한 단계 하락 |
| 상태 | 2단계 (`answeredAt` 파생) | 단발 구조에 6단계 enum은 과설계 |
| 잔재 기능 | `Category`(→`Topic`)만 존치 | 강좌 구분·긴급도는 강사 1명·학생 자가신고 맥락에서 무의미 |
| 접근 제한 | 없음 + 레이트리밋 | 마찰 최소화 우선. 오염 발생 시 참여코드로 즉시 전환 가능하게 설계 |
| 예약 | v2 이연 | 질문과 성격이 정반대인 별도 앱. v1 검증 후 착수 |
| 배포 | 미정 (플랫폼 중립, DB 커넥션만 예외) | 첨부 제외로 디스크 종속이 사라져 결정을 미룰 수 있게 됨 |
