# 핸드오프 — 강사 질문방

작성: 2026-08-16
작업 디렉터리: `D:\07.AI(웹)\instructor-qna` (git `main`, 워킹트리 clean)

---

## 1. 지금 상태 한 줄

**v1 기능 전부 동작. 운영 배포 완료. 미해결 이슈 없음.**

https://instructor-qna.vercel.app

---

## 2. 기존 문서 (중복 작성 금지 — 먼저 읽을 것)

| 파일 | 내용 |
|---|---|
| `PRD.md` | 기획서 v1.1. 설계 결정과 근거가 §13에 있다. 구조 변경 전 필독 |
| `AGENTS.md` | 코드 수정 시 지켜야 할 보안 규칙 5가지 |
| `DEPLOY.md` | 배포 절차 (Vercel + GitHub, CLI 없이 웹 경로) |
| `WORKFLOW.md` | 이 프로젝트를 만든 전 과정과 사용한 프롬프트 |
| `README.md` | 로컬 실행 방법, 환경변수 목록 |

이 핸드오프에는 위 내용을 복사하지 않았다.

---

## 3. 해결 완료 — 배포 환경에서만 터진 결함 3건

전부 "로컬에서는 되는데 배포하면 안 되는" 유형이었다. 같은 실수를 반복하지 않기 위해 남긴다.

### ① DB 접속 실패 — 환경변수 값 오염

Vercel에 붙여넣은 `DATABASE_URL` 에 앞뒤 큰따옴표가 딸려 들어갔다.
값을 다시 넣고 Redeploy 하여 해결.

**교훈:** Vercel 환경변수는 따옴표 없이 넣는다. 값 변경 후에는 반드시 Redeploy 해야 반영된다.

### ② 표시 시각 9시간 오차 — `7722639`

`formatDateTime` 이 `getHours()` 등 서버 로컬 시간 기준 메서드를 썼다.
개발기가 KST라 드러나지 않다가 UTC로 도는 Vercel에서 노출됐다.
`Intl.DateTimeFormat` 의 `timeZone: "Asia/Seoul"` 로 고정.

**규칙:** 표시 시각은 `src/lib/constants.ts` 의 포맷 함수만 사용할 것. 로컬 기준 메서드 금지.

### ③ 이메일 전량 발송 거부 — `15f4340`, `e4da333`

두 결함이 겹쳐 있었다.

**(가) 실패가 조용히 묻힘** — Resend SDK 는 API 오류를 예외로 던지지 않고
`{ data, error }` 로 반환한다. `try/catch` 만 두고 반환값을 확인하지 않아
어떤 실패도 로그·화면에 남지 않았다.
→ 반환된 `error` 를 확인하고 `Settings.lastEmailError` 에 기록하도록 수정.
   강사 설정 화면에 **이메일 발송 상태** 패널 추가.

**(나) 빈 문자열 폴백** — Vercel 에 `EMAIL_FROM` 변수가 값 없이 등록돼 있었다.
`process.env.X ?? 기본값` 은 `""` 에 대해 기본값을 쓰지 않는다.
발신 주소가 `강사 질문방 <>` 가 되어 Resend 가 전량 거부했다.

```
validation_error: Invalid `from` field ... (from=강사 질문방 <>)
```

→ `src/lib/env.ts` 의 `envOr` / `hasEnv` 추가. 공백만 있는 값도 미설정으로 취급.
   `EMAIL_FROM`, `RESEND_API_KEY`, `SESSION_SECRET` 에 적용.

**규칙:** 환경변수를 읽을 때 `??` 를 쓰지 말고 `envOr` 를 쓸 것.

### 진단 수단 (앞으로 활용)

사용자는 Vercel 로그 접근을 어려워한다.
이메일 문제는 **강사 화면 → 설정 → 이메일 발송 상태** 패널에서 바로 확인 가능하다.
이 패널이 원인 특정을 즉시 해줬다.

---

## 4. 검증 완료된 것

운영 환경에서 실제로 확인했다.

| 항목 | 상태 |
|---|---|
| 사이트 접속 · 전 라우트 200 | ✅ |
| DB 연결 (Neon) | ✅ |
| 질문 유형 4종 렌더링 | ✅ |
| 질문 제출 → 저장 → 번호 발급 | ✅ |
| `robots.txt` 색인 차단 | ✅ |
| 강사 로그인 | ✅ (사용자 확인) |
| 이메일 발송 | ✅ (사용자 수신 확인) |
| 표시 시각 KST | ✅ |

로컬에서 추가로 검증한 것 (운영 미검증):

- 강사 로그인 → 대기 목록 → 답변 등록 → 학생 화면 반영
- **소유권 검증 6/6 통과** — 학생 2명을 만들어 교차 접근 시험. 타인 질문 접근은 전부 404

---

## 5. 아직 안 한 일

| 할 일 | 비고 |
|---|---|
| **도메인 등록** | 학생에게 메일이 가려면 필수. 아래 §6 |
| Neon DB 비밀번호 교체 | 작업 중 노출됨. Resend 키는 이미 교체 완료 |
| 로컬 `.env`의 `RESEND_API_KEY` 갱신 | 선택. 없으면 터미널에 메일 내용이 출력된다 |
| Vercel `EMAIL_FROM` 값 채우기 | 선택. 비어 있어도 코드가 기본값으로 처리한다 |
| 테스트 데이터 정리 | 질문 6건, 학생 3명. 강사 화면에서 삭제 가능 |

---

## 6. 도메인 미등록의 영향 (설계상 중요)

발신 주소가 `onboarding@resend.dev`인 동안 Resend는 **계정 소유자 본인 주소로만** 발송한다.

- 강사 알림 → 도착
- **학생 알림 → 차단**

PRD §11의 핵심 결정 중 하나가 "학생이 답변 도착을 모르면 플랫폼을 우회한다"였다.
도메인 없이 학생에게 열면 그 전제가 무너진다. **실사용 전 도메인 등록이 사실상 필수다.**

---

## 7. 환경 메모

- Windows 11 / PowerShell 주 셸. 경로에 한글·괄호 포함 (`D:\07.AI(웹)\`)
- `gh` CLI **미설치**. winget 설치 시도는 권한 승인 창에서 멈춤(600초 무응답) → 포기
- `vercel` CLI 설치돼 있으나 **로그인 불가**: 한글 경로/장치명 때문에
  `Cannot convert argument to a ByteString ... value of 50676` 발생 → 웹 UI 경로 사용
- git push는 정상 동작 (Git Credential Manager에 자격증명 저장됨)
- git identity: `passionAIlab <ailab1004jskim@gmail.com>` (전역)
- 시크릿은 이 문서에 일절 포함하지 않음. 값은 `.env`(로컬)와 Vercel 환경변수에만 존재

---

## 8. 사용자 응대 시 주의

- **비전공자다.** 용어를 풀어서 쓰고, 화면 위치를 단계별로 짚어줄 것
- `.env`와 `.env.example`을 **세 번 혼동**했다. 실제 값을 견본 파일에 넣는 실수가 반복됐다.
  값 전달이 필요하면 "`.env.example`에 넣으면 내가 옮기겠다"는 방식이 실제로 통했다
- 명령을 붙여넣을 때 **같은 줄에 하실 말씀을 이어서 타이핑**하는 경우가 있다.
  `notepad "...\.env" 파일을 찾을수 없어` → 명령이 깨져 "파일 없음"으로 보였다
- 파일을 직접 열기 어려워한다. **문서는 프로젝트 폴더에 두고 GitHub 링크를 함께 안내**할 것
- 키·비밀번호는 대화창에 붙여넣지 말라고 안내했으나 두 번 붙여넣었다. 노출 시 즉시 교체를 권할 것

---

## 9. 제안 스킬

| 스킬 | 언제 |
|---|---|
| `vercel:env-vars` | 환경변수 문제 진단 — **다음 세션 첫 작업** |
| `vercel:deployments-cicd` | 배포·재배포 절차 확인 |
| `vercel:vercel-cli` | CLI 로그인이 되면 로그 조회가 훨씬 빨라진다 |
| `vercel:nextjs` | 코드 수정 시. 이 버전은 학습 데이터와 다르므로 `node_modules/next/dist/docs/` 필독 |
| `code-review` | 기능 추가 후 |
| `grill-me` | v2(상담 예약) 착수 시. PRD §11에 답할 질문 8개가 정리돼 있다 |

---

## 10. 커밋 이력

```
8995030  작업 워크플로우 기록 추가 (WORKFLOW.md)
0365116  임시 진단 경로 제거
924f176  운영 DB 접속 실패 원인 확인용 임시 진단 경로 추가
c37c872  환경변수 반영을 위한 재배포 트리거
7254948  배포 절차서 추가
882945a  서버리스 배포 대비 DB 연결을 풀러/직결로 분리
8528dcc  이메일 개발 폴백 추가 및 화면 다듬기
f51c85d  DB 초기 마이그레이션 및 시드 실행 환경 구성
a0d2c68  강사 Q&A 플랫폼 초기 스캐폴딩
```

원격: https://github.com/ailab1004jskim-lang/-instructor-qna
